/**
 * AMD module for multi-competency question mapping.
 *
 * Listens for changes on `.competency-multiselect` elements (rendered by
 * competency_column) and sends the full selected set of competency IDs to
 * the web service as an array, replacing all existing mappings atomically.
 *
 * @module      qbank_competency/mapping
 * @copyright   2026 Mahmoud Salem
 * @copyright   based on work by 2026 Hakan Çiğci {@link https://hakancigci.com.tr}
 * @license     http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
import Ajax from"core/ajax";import Notification from"core/notification";const getSelectedIds=e=>Array.from(e.selectedOptions).map(e=>parseInt(e.value,10)),debounce=(e,t)=>{let o;return(...c)=>{clearTimeout(o),o=setTimeout(()=>e(...c),t)}};export const init=()=>{document.addEventListener("change",debounce(async e=>{const t=e.target;if(!t.classList.contains("competency-multiselect"))return;const o=parseInt(t.dataset.questionid,10),c=parseInt(t.dataset.courseid,10),n=getSelectedIds(t);try{await Ajax.call([{methodname:"qbank_competency_save_question_competency",args:{questionid:o,competencyids:n,courseid:c}}])[0]}catch(e){Notification.exception(e)}},400))};
//# sourceMappingURL=mapping.min.js.map